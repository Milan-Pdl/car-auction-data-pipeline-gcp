buyonline_pipeline/
│
├── scraping/
│   └── scrape_listings.py          ← (1) Web scraping → DataFrame → CSV (local temp)
│
├── gcp_upload/
│   └── upload_to_gcs.py            ← (2) Upload CSV → Google Cloud Storage
│
├── db_load/
│   └── load_to_mysql.py            ← (3) Load CSV → MySQL (with duplicate check)
│
├── kafka_publish/
│   └── publish_to_kafka.py         ← (4) Publish to Kafka Pub/Sub (based on config)
│
└── dags/
    └── buyonline_dag.py            ← (5) Airflow DAG (runs 1 → 2 → 3 → 4 sequentially)